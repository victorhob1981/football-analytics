name: "football_analytics_v2"
version: "1.0.0"
config-version: 2

profile: "football_pg_v2"

model-paths: ["models"]
test-paths: ["tests"]

models:
  football_analytics_v2:
    serving:
      +schema: serving_v2
      +materialized: table

clean-targets:
  - "target"
